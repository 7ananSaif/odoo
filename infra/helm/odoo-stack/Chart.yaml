apiVersion: v2
name: odoo-stack
description: Helm chart for the Odoo ERP stack (Postgres, ConfigMap, Secret, PVCs) with resource requests/limits and readiness/liveness probes.
type: application
version: 0.1.0
appVersion: "17.0"
kubeVersion: ">=1.28.0"
